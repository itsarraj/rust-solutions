# as we are using standard error for error output
```bash
# error in cli and output.txt is empty
cargo run > output.txt
# error in cli and success results are in output.txt
cargo run -- to poem.txt > output.txt 
```
